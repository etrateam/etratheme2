{#
| Variable         | Type       | Description
|------------------|------------|------------
| page             | object     |
| page.title       | string     | *could be html
| page.slug        | string     | ex: "cat.show"
#}

{% extends "layouts.master" %}

{% block content %}
  {# (اختياري) شريط اختبار بسيط - احذفيه لاحقاً #}
  <div style="padding:8px;background:#fffbcc;border:1px solid #f0e68c;margin:8px 0">ETRA TEST</div>

  {# (اختياري) مقطع الهيرو — علّقيه إذا شكّيتي فيه #}
  {# {% include "components/home/hero.twig" %} #}

  {# المهم: هذا الذي يُظهر عناصر التخصيص في محرر سلة #}
  {% component home %}
{% endblock %}

{% block scripts %}
  <script defer src="{{ 'home.js' | asset }}"></script>
{% endblock %}
